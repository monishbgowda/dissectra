- [x] Update CaptureScreen.tsx: fix multi-image state sync between `asset` and `assets`.
- [x] Update CaptureScreen.tsx: enable Analyze button based on `assets.length` not `asset` only.
- [x] Update CaptureScreen.tsx: ensure gallery/camera multiCapture set `asset` to first selected item.
- [ ] Verify no TypeScript/formatting issues (best-effort) and re-read CaptureScreen.tsx after edits.


